/* REALLY VERY GOOD */

INITIALIZE()=
DECLARE disp_text = VARCHAR(80) NOT NULL

{
SELECT text AS disp_text 
FROM aa_aardvark_message;

MESSAGE 'This is version v1.10'
WITH STYLE = POPUP;

MESSAGE :disp_text
WITH STYLE = POPUP;

}
'End',KEY FRSKEY6 =
{
RETURN;
}
